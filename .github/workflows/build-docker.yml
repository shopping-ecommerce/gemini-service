name: Docker Publish (gemini-service)

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]

env:
  IMAGE_NAME: gemini-service

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Create credentials file from GitHub secret
      - name: Create credentials file
        run: |
          mkdir -p ./gemini-service
          echo "${{ secrets.GCP_CREDENTIALS_JSON }}" > ./gemini-service/credentials.json
          ls -la ./gemini-service/credentials.json

      - name: Docker meta (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha
            type=raw,value=latest
            type=semver,pattern={{version}}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: ./gemini-service
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # Clean up credentials
      - name: Clean up
        if: always()
        run: rm -f ./gemini-service/credentials.json
